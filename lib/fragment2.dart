import 'package:flutter/material.dart';

class MysecondFrag extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(
      child: Text(
        'second Fragment',
        style: TextStyle(fontSize: 30.0),
      ),
    );
  }
}
