import 'package:flutter/material.dart';

class MyfirstFrag extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(
      child: Text(
        'First Fragment',
        style: TextStyle(fontSize: 30.0),
      ),
    );
  }
}
