import 'package:flutter/material.dart';

class MythirdFrag extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(
      child: Text(
        'Third Fragment',
        style: TextStyle(fontSize: 30.0),
      ),
    );
  }
}
